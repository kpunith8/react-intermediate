import React, { Component } from 'react';

/**
 * It can be any SVG icon, use switch it to select the icon based on name prop passed to Icon component
 */
class Icon extends Component {
  render() {
    return (
      <div>

      </div>
    );
  }
}

export default Icon;